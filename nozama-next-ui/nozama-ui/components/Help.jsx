import React from "react";

import { Slider } from "./Slider";
import { Stack, Ratio } from "react-bootstrap";

const Help = (props) => {
  return (
    <Stack gap={4}>
      <Slider />
    </Stack>
  );
};

export default Help;
