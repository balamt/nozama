## NOZAMA_SERVICE_PROFILE value from the OS Environment variable value, if no set it takes default value "dev"
spring:
    profiles:
      active: ${NOZAMA_SERVICE_PROFILE:dev}

springdoc:
  api-docs:
    path: /order/v3/api-docs
  swagger-ui:
    path: /order/swagger-ui.html